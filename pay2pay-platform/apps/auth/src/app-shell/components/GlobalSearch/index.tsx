import React from "react";
import { SearchInput } from "@/design-system/components";

export const GlobalSearch: React.FC = () => (
  <SearchInput placeholder="Search module, customer, account or transaction (Ctrl+K)..." />
);
